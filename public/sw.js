// service worker = 페이지와 별개로 백그라운드에서 도는 스크립트.
// 브라우저와 네트워크 사이에 앉아서 이 사이트의 모든 요청을 가로챌 수 있다.
// (CLAUDE.md의 "개입 계층"과 같은 종류다 — 우리 코드가 부르지 않는데 자동으로 끼어든다.
//  다만 이건 ①브라우저 안에서 돈다)
//
// 왜 두는가: 안드로이드/데스크톱 Chrome은 service worker가 없으면 "설치 가능한 앱"으로
// 인정하지 않아서 설치 아이콘을 안 띄운다. 아이폰은 이 파일과 무관하다.
//
// ponytail: 캐시를 안 한다. 이 앱의 화면은 전부 "지금" 데이터(핀·사진·지도 타일)라
// 캐시된 화면은 느린 화면보다 나쁘다 — 어제 파티가 오늘 열린 것처럼 보인다.
// 오프라인 지원이 필요해지면 그때 정적 파일만 골라 캐시할 것.
//
// respondWith를 부르지 않으면 브라우저가 평소대로 요청을 처리한다.
// 즉 이 핸들러는 아무 일도 안 하지만, "존재한다"는 것 자체가 설치 조건이다.
self.addEventListener("fetch", () => {});
